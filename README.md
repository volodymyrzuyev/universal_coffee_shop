Universal Coffee Shop App - React Native Front End, Python Back End.
Install commands:
npm install (in front end foler)
python install pip
pip install unicorn
Run backend:
uvicorn main:app --host 0.0.0.0 --port 8080
